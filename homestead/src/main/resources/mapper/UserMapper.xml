<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.blmdz.home.dao.UserDao">
	<resultMap id="BaseResultMap" type="cn.blmdz.home.model.User">
		<id column="id" property="id" />
        
        <result column="nick" property="nick" />
        <result column="avatar" property="avatar" />
        <result column="vip" property="vip" />
        <result column="level" property="level" />
        <result column="active_day" property="activeDay" />
        <result column="gender" property="gender" />
        <result column="sign_date" property="signDate" />
        <result column="login_time" property="loginTime" />
        
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="cloums_all">
		id, <include refid="cloums" />
	</sql>

	<sql id="cloums">
		nick, avatar, vip, level, active_day, gender, sign_date, login_time, status, create_time, update_time
	</sql>

	<sql id="vals_all">
		#{id}, <include refid="vals" />
	</sql>

	<sql id="vals">
		#{nick}, #{avatar}, #{vip}, #{level}, #{activeDay}, #{gender}, #{signDate}, #{loginTime}, true, now(), now()
	</sql>

	<sql id="tb">
		tb_user
	</sql>

	<insert id="create" useGeneratedKeys="true" keyProperty="id" parameterType="cn.blmdz.home.model.User">
		insert into
		<include refid="tb" />
		(<include refid="cloums" />)
		values
		(<include refid="vals" />)
	</insert>

	<delete id="delete" parameterType="long">
		delete from
		<include refid="tb" />
		where id = #{id}
	</delete>

    <delete id="tombstone" parameterType="long">
        update
        <include refid="tb" />
        <set>
            status = false
        </set>
        where id = #{id}
    </delete>

	<update id="update" parameterType="cn.blmdz.home.model.User">
		update
	    <include refid="tb"/>
		<set>
			<if test="nick != null">nick = #{nick},</if>
			<if test="avatar != null">avatar = #{avatar},</if>
			<if test="vip != null">vip = #{vip},</if>
			<if test="level != null">level = #{level},</if>
			<if test="activeDay != null">active_day = #{activeDay},</if>
			<if test="gender != null">gender = #{gender},</if>
			<if test="signDate != null">sign_date = #{signDate},</if>
			<if test="loginTime != null">login_time = #{loginTime},</if>
          	update_time = now()
		</set>
		where id = #{id}
	</update>

	<select id="findById" parameterType="long" resultMap="BaseResultMap">
		select
		<include refid="cloums_all" />
		from <include refid="tb" />
		where id = #{id}
	</select>

	<select id="list" resultMap="BaseResultMap">
		select
		<include refid="cloums_all" />
		from <include refid="tb" />
	</select>

	<select id="page" resultMap="BaseResultMap">
		select
		<include refid="cloums_all" />
		from <include refid="tb" />
	</select>
</mapper>
